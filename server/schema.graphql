type Chat {
  id: Int!
  writer: String!
  description: String!
  roomName: String
}
type Query {
  chatting(roomName: String!): [Chat]!
}
type Mutation {
  write(writer: String!, description: String!, roomName: String!): String!
}
type Subscription {
  newChat(roomName: String!): Chat
}